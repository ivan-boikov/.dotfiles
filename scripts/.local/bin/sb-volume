#!/bin/sh

# Prints the current volume or MUT if muted.

[ $(pamixer --get-mute) = true ] && echo "MUT" && exit

vol="$(pamixer --get-volume)"

if [ "$vol" -gt "0" ]; then
	icon=""
else
    echo "MUT" && exit
fi

printf "%3d" $vol
